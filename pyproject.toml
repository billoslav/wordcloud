[tool.ruff]
target-version = "py38"
line-length = 100
extend-exclude = [
    "Results",
    "Tracking",
    "benchmarks/outputs",
    "fonts",
    "__pycache__",
]

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
